# Iteration 10 - Orient
## Analysis
Since 0.2.0 is the first standalone release, the migration guide primarily serves users moving from the internal EdgeCode module to the standalone crate. Key differences: import paths, error types, feature flags, new modules, trait changes (tool methods with defaults), LLMResponse new fields, StreamChunk enum.
