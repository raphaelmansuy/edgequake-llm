# Iteration 15 - Orient
Both modules are relatively small. tokenizer is close to full coverage. genai_events needs emit_inference_event tested (but requires careful env var setup).

# Iteration 15 - Decide
Add tests for all remaining branches in tokenizer.rs and genai_events.rs.
