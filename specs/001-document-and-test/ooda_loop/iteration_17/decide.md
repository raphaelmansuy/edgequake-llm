# Iteration 17 - Decide
Add 16 tests: TTL expiration (completion + embedding), LRU eviction (completion + embedding), CachedProvider (name/model/complete/embed/clear/with_default), access_count, empty keys, overwrite, clear eviction count, config builder.
